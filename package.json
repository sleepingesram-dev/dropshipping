{
  "name": "dropship-os",
  "version": "0.1.0",
  "private": true,
  "description": "DropshipOS — automation command center for a dropshipping business: store health, ad performance, order automation, finance, and scaling signals in one dashboard.",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client",
    "start": "npm run start -w server",
    "test": "npm test -w server",
    "migrate": "npm run migrate -w server"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
